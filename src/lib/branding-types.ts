export type BrandingConfig = {
  companyName: string;
  productName: string;
  subtitle: string;
  primaryColor: string;
  primaryDarkColor: string;
  sidebarColor: string;
  accentColor: string;
  logoUrl: string | null;
  updatedAt: string | null;
};
